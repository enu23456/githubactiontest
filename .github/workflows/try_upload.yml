name: Upload file

on:
  workflow_dispatch:

jobs:
 upload:
 
    runs-on: ubuntu-latest
    steps:
      - run: ls
      - uses: actions/upload-artifact@v2
        with:
          name: README_uploaded
          path: README.md
